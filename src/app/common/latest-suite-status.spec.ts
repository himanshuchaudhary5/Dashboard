import { LatestSuiteStatus } from './latest-suite-status';

describe('LatestSuiteStatus', () => {
  it('should create an instance', () => {
    expect(new LatestSuiteStatus()).toBeTruthy();
  });
});
